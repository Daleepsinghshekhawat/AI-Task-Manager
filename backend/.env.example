# API Server Configuration
PORT=5000
NODE_ENV=development

# Database Configuration (SQLite uses file storage by default)
DB_PATH=./taskmanager.sqlite

# Logging Level: 'error', 'warn', 'info', 'debug'
LOG_LEVEL=info
